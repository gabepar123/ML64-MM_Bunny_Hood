import IMemory from './IMemory';
export declare class Heap {
    free_head: number;
    free_tail: number;
    used_head: number;
    start: number;
    end: number;
    size: number;
    emulator: IMemory;
    constructor(emulator: IMemory, start?: number, size?: number);
    malloc(size: number): number;
    free(address: number): void;
    realloc(address: number, size: number): number;
    wipe(): void;
    get_free(): number;
    get_largest_free(): number;
}
